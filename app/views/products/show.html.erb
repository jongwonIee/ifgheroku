<p>
  <% unless @product.nil? %>
    <% @product.each do |product| %>
      <img src="<%= product.URL %>"> <br />
      <%= product.title_ko %>의 첫 주 예상 관객 수는 <br />
      <%= (product.expectation).round(0) %>명 입니다. <br />
      실제 관객 수: <%= product.fweek %> <br />
      오차율: <%= (product.percent_error).round(1) %>%
    <% end %>
  <% else %>
      검색어를 입력해 주세요.
  <% end %>
</p>